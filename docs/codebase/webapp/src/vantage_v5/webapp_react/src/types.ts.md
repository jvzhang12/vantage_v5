# `src/vantage_v5/webapp_react/src/types.ts`

Shared TypeScript types for the React frontend.

## Purpose

- Define typed app state, normalized turn payloads, surfaces, auth, account-creation-code status, workspace, and receipt/provenance models.

## Coverage

- Surface kinds, app views, health/workspace payloads, chat history, source refs, surface invocation, additive surface actions, answer basis, response mode, recall items, workspace updates, context budget, activity, semantic frame/policy, write/action payloads, and reducer state.
